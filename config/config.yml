vector_db:
  index_name: "agent-trading-bot"

retriever:
  top_k: 3
  score_threshold: 0.5

embedding_model:
  provider: "google"
  model_name: "models/text-embedding-004"

llm:
  google:
    provider: "google"
    model_name: "gemini-2.5-flash-lite"

  groq:
    provider: "groq"
    model_name: "moonshotai/kimi-k2-instruct"

tools:
  tavily:
    max_result: 5